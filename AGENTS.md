Follow TDD development flow.

Continue implementing the current plan until all tasks are complete.
Delete task files only once complete.
Delete plan folders only once complete.

CI will run tests and style so do so prior to commits.
